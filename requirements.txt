# ── Core ─────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# ── Database ─────────────────────────────────
pymongo>=4.6.0

# ── AI / LLM ────────────────────────────────
groq>=0.4.0
# google-genai>=1.0.0          # Uncomment to use Gemini backend

# ── HTTP client (Deepgram API calls) ────────
httpx>=0.27.0

# ── Speech-to-Text (Deepgram Nova-3 via httpx — no extra package needed)
# ── Text-to-Speech (Deepgram Aura via httpx — no extra package needed)

# ── Backup STT/TTS (uncomment to use) ───────
# elevenlabs>=1.0.0
# gTTS>=2.5.0

# ── WebSocket ────────────────────────────────
websockets>=12.0

